services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: harke_backend
    ports:
      - "${PORT:-8001}:8001"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=8001
    restart: unless-stopped
