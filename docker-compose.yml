services:
  # Backend Service
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: restaurant_backend
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      # App Config (defaults if not in .env)
      - PORT=8000
    restart: unless-stopped

